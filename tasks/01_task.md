# Tarefa 01 — Implementação mínima

Objetivo: implementar `process(x)` isolado e testável.

Passos resumidos:
- Criar `src/tasks/task01.py` com `process(x)` que retorna `x * 2`.
- Criar `tests/tasks/test_task01.py` com teste unitário.
- Rodar `pytest` e commitar.

Critério de aceitação:
- `pytest tests/tasks/test_task01.py` passa.
